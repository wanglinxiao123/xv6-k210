# 结构体
```c
struct spinlock tickslock; // 用于保护变量 ticks
uint ticks;
```
# 函数接口
```c
// 初始化保护变量 ticks 的自旋锁
void timerinit()

// ticks++，设置下次定时时间
void timer_tick()


devintr()-> 定时器中断
timer_tick()->
set_next_timeout()->
SBI_CALL_1(SBI_SET_TIMER, stime_value)-> 设置下一次的定时时间
```

